#ifndef UTILITY_H
#define UTILITY_H

#include <vector>
#include <string>

class Utility {
public:
    static std::vector<std::string> split(const std::string &text, char sep);
};

#endif //UTILITY_H

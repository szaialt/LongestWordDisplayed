This program is written for Windows XP and above because it uses 
Windows API. GUI elements are given in main.cpp, findworddialog 
and offsetdialog files (.h and .rc).
It works only with ANSI-encoded files.
It uses a temporarly file named tmp.txt for owerwriting.
